services:
  - type: web
    name: chatbot
    env: python
    plan: free
    buildCommand: >-
      pip install -r requirements.txt &&
      python -m nltk.downloader punkt stopwords -d chatbot/nltk_data
    startCommand: gunicorn chatbot.wsgi:app --workers 1 --threads 4 --timeout 120
